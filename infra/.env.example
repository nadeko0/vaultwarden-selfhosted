# Copy to .env and fill in, then: chmod 600 .env
# .env is git-ignored - never commit the real one.

DUCKDNS_SUBDOMAIN=your-subdomain
DUCKDNS_TOKEN=your-duckdns-token
DOMAIN=https://your-subdomain.duckdns.org

# Generate with: openssl rand -base64 48
ADMIN_TOKEN=
